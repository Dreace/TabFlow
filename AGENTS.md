# SwiftUI 项目代码编写规范

## 核心要求

1. 以猜测接口为耻，以查阅定义和文档为荣。
2. 以模糊执行为耻，以确认关键约束为荣。
3. 以臆测业务为耻，以依据需求和现有实现为荣。
4. 以随意创建接口为耻，以复用现有能力为荣。
5. 以跳过验证为耻，以主动测试为荣。
6. 以破坏架构为耻，以遵循项目规范为荣。
7. 以假装理解为耻，以明确说明未知为荣。
8. 以盲目修改为耻，以谨慎重构为荣。

不要使用比喻。专注完成任务，不说废话，不使用没有实际意义的术语。

应主动分析相关影响并一次性完成任务，不要等待逐条指示。

---

## Tooling & Shell Usage

本节仅适用于能够执行命令和修改文件的编码代理。

1. 优先使用内置的文件读取和写入工具。
2. 搜索文件和代码时优先使用 `rg` 和 `rg --files`，不可用时再使用其他工具。
3. 版本控制操作使用 `git` 和 `gh`。
4. JSON 处理使用 `jq`。
5. Swift Package Manager 操作使用 `swift package`、`swift build` 和 `swift test`。
6. Xcode 项目构建和测试使用 `xcodebuild`。
7. 禁止使用 Python、Perl 或临时脚本批量修改项目文件。
8. 避免使用仅适用于 PowerShell 的命令。
9. 不要直接手工修改 `.xcodeproj/project.pbxproj`，除非项目没有使用 XcodeGen、Tuist、Swift Package Manager 等可维护的工程配置方式，并且任务确实要求修改工程文件。
10. 项目使用 XcodeGen 或 Tuist 时，应修改其源配置并重新生成工程，不要仅修改生成产物。

---

## 任务执行规则

1. 开始修改前，先检查需求、相关代码、依赖关系和项目约束，明确完成步骤。
2. 使用任务列表管理多步骤任务，并在执行过程中及时更新状态。
3. 一次性完成给定任务，不因任务较长而中断。
4. 修改完成后必须检查实际改动，确认结果符合需求。
5. 不确定 API 用法时，优先按以下顺序确认：

   * 查看项目中的现有调用方式。
   * 查看 SDK 或依赖源码中的类型定义。
   * 查看 Apple 官方文档或依赖库官方文档。
   * 必要时搜索可靠的一手资料。
6. 不得假设某个类型、方法、系统版本或第三方接口存在。
7. 修改前先确认项目支持的最低系统版本、Swift 版本、Xcode 版本和依赖版本。
8. 严禁修改任务范围之外的文件内容。
9. 不要顺手格式化、重命名或重构无关代码。
10. 如果代码在任务执行期间被人工修改，应以当前磁盘内容为准，重新分析后继续，不得覆盖人工修改。
11. 发现现有实现与需求冲突时，应明确指出冲突，并采用影响范围最小的方案。
12. 必要时使用 SwiftUI、Swift Concurrency、SwiftData、Core Data 或项目指定框架的专项工具和文档。

---

## 代码编写规则

编写 Swift 和 SwiftUI 代码时必须遵守以下原则。

### 基础规范

1. 遵循项目现有的 Swift 编码风格、命名方式和目录结构。
2. 优先使用 Swift 原生类型、协议和标准库能力。
3. 代码必须简洁、明确，避免无意义的封装和过度抽象。
4. 新增抽象前，应确认至少存在明确的复用场景或架构边界需求。
5. 默认使用值类型。没有共享可变状态或引用语义需求时，不要使用 `class`。
6. 领域模型优先使用不可变的 `struct`：

   * 可使用 `let` 的属性不得使用 `var`。
   * 根据实际需要遵循 `Equatable`、`Hashable`、`Codable`、`Identifiable` 和 `Sendable`。
   * 不要为了方便而无条件添加协议。
7. 枚举应覆盖完整状态，避免使用没有约束力的字符串表示业务状态。
8. 不得使用强制解包、强制类型转换或 `try!`，除非该位置已通过静态条件保证绝对安全，并附有清晰原因。
9. 不得吞掉错误。禁止无说明地使用空的 `catch`。
10. 错误应在合适的层级转换为明确的领域错误或展示状态。
11. 访问控制应尽可能收紧，优先使用 `private` 和 `fileprivate`，仅暴露实际需要的接口。
12. 删除无效代码、未使用变量、无意义注释和调试输出。
13. 不要保留被注释掉的旧实现，版本历史应交给 Git 管理。
14. 日志统一使用项目现有日志设施；没有现成设施时优先使用 `Logger`，不要在正式代码中散落 `print`。
15. 固定文案不得直接散落在 View 中，应优先进入本地化资源。

### SwiftUI 规则

1. 页面布局和业务逻辑遵循 MVVM，保持 View、ViewModel、Model 之间边界清晰。
2. View 只负责：

   * 声明界面结构。
   * 展示状态。
   * 转发用户交互。
   * 维护仅与界面呈现有关的短生命周期状态。
3. ViewModel 负责：

   * 页面状态。
   * 异步任务。
   * 用户操作处理。
   * 调用 Use Case 或 Repository。
   * 将领域数据转换为页面可展示状态。
4. 领域和数据访问逻辑不得直接写入 View。
5. 不要在 `body` 中执行网络请求、数据库读取、重计算或产生副作用。
6. View 的 `body` 应保持可预测，不得依赖隐藏的全局可变状态。
7. 页面首次加载等异步操作优先使用 `.task`，并确保任务能够正确取消。
8. 使用 `.task(id:)` 时，应确认状态变化不会导致重复请求或任务循环。
9. 使用 `onAppear` 时必须考虑重复触发，不得默认其只执行一次。
10. ViewModel 的状态管理方式应遵循项目现有方案：

    * 使用 Observation 框架的项目优先使用 `@Observable`。
    * 现有项目使用 Combine 时，继续使用 `ObservableObject` 和 `@Published`。
    * 不得在同一模块中无理由混用多套状态管理方案。
11. 需要在主线程更新界面状态的 ViewModel 应标记为 `@MainActor`，或通过明确的主线程隔离保证安全。
12. View 不应直接持有数据库上下文、网络客户端、DAO 或具体 Repository 实现。
13. 不要创建体积过大的 View。复杂页面应拆分为语义清晰的子 View。
14. 仅在真正需要独立复用或降低复杂度时拆分组件，不要将每一小段布局机械拆成文件。
15. 不要使用 `AnyView` 隐藏类型问题，除非确实需要类型擦除。
16. `GeometryReader` 仅在布局确实依赖容器尺寸时使用，不得作为默认布局工具。
17. 优先使用 SwiftUI 原生布局系统，例如：

    * `VStack`
    * `HStack`
    * `ZStack`
    * `Grid`
    * `Layout`
    * `safeAreaInset`
    * `containerRelativeFrame`
18. 避免依赖固定屏幕尺寸和 `UIScreen.main.bounds` 进行布局。
19. 必须考虑动态字体、深色模式、横竖屏、安全区域和不同设备尺寸。
20. 交互控件应使用 `Button`、`Toggle`、`NavigationLink` 等语义组件，不要用 `onTapGesture` 替代标准控件。
21. 页面必须考虑无障碍：

    * 设置正确的无障碍标签和提示。
    * 支持动态字体。
    * 不得仅通过颜色表达状态。
    * 确保可点击区域尺寸合理。
22. Preview 中不得直接调用真实网络、真实数据库或依赖生产环境数据。
23. Preview 应使用稳定的 Mock、Stub 或内存数据源。

### Swift Concurrency 规则

1. 异步代码优先使用 `async/await`，不要在同一业务流程中无理由混用回调、Combine 和 Swift Concurrency。
2. 并发任务必须考虑取消。
3. 捕获 `self` 前应判断生命周期和引用关系，不要机械使用 `[weak self]`。
4. 非必要不得创建脱离结构化并发的 `Task.detached`。
5. 不得通过随意创建 `Task` 规避 actor 隔离或编译器并发检查。
6. 共享可变状态必须通过 actor、主线程隔离或其他明确的同步机制保护。
7. 跨并发边界传递的类型应满足 `Sendable` 要求。
8. 不要无理由使用 `@unchecked Sendable`。确需使用时必须说明线程安全依据。
9. 异步方法应明确错误传播和取消行为。
10. 对搜索、输入联想等高频任务，应取消旧任务，避免过期结果覆盖新状态。
11. 多个互不依赖的异步操作可以使用 `async let` 或任务组并发执行，但必须保证错误和取消行为正确。
12. 不要使用固定延迟模拟同步或依赖任务执行顺序。

---

## 架构规则

### MVVM

1. View 只负责渲染和转发交互。
2. ViewModel 管理页面状态和页面行为。
3. Model 表示领域数据或页面展示数据。
4. ViewModel 不得依赖具体数据库实现。
5. 页面之间不得直接访问彼此的 ViewModel。
6. 跨模块通信应通过路由、领域接口、共享服务或明确的数据流完成。

### Repository Only Rule

1. View 和 ViewModel 禁止直接访问以下对象：

   * `ModelContext`
   * `NSManagedObjectContext`
   * SwiftData Model
   * Core Data Managed Object
   * DAO
   * 数据库客户端
   * 网络客户端
   * 文件存储实现
2. ViewModel 只能依赖 Domain 层定义的 Repository 协议或 Use Case。
3. 数据读取和写入必须经过 Repository。
4. 数据库访问仅允许出现在 Data 层的 Repository 实现和 Persistence 模块内部。
5. Repository 对上层返回领域模型，不得直接暴露数据库实体。
6. 网络 DTO、数据库实体和领域模型必须保持边界清晰。
7. 映射逻辑应放在 Data 层或专门的 Mapper 中，不得散落在 View 中。

### 依赖注入

1. 依赖应通过初始化方法、环境值或项目已有依赖容器注入。
2. 不得在 ViewModel 内直接创建具体 Repository、数据库或网络客户端。
3. 不得通过全局单例隐藏依赖，除非该单例是项目明确规定的基础设施。
4. 测试中应能够替换 Repository、时钟、UUID 生成器和其他不稳定依赖。
5. 新增依赖前先检查项目是否已有同类服务或注册入口。

---

## 数据模型规则

1. 领域实体放在 Domain 层，不依赖 SwiftUI、SwiftData、Core Data 或具体网络库。
2. 领域实体优先定义为不可变 `struct`。
3. SwiftData 的 `@Model` 类型或 Core Data 的 `NSManagedObject` 仅属于 Data/Persistence 层。
4. 不得将数据库实体直接传入 View 或 ViewModel。
5. 网络 DTO 仅用于网络边界，不得直接作为领域实体使用。
6. 日期、金额、标识符等重要业务类型应使用明确的数据类型，不要全部使用 `String`。
7. 对存在业务约束的标识符，可使用轻量值类型封装，避免不同 ID 混用。
8. 修改持久化字段前，必须先确认项目当前的数据迁移策略。
9. 当前阶段如果项目明确允许破坏性修改，可直接调整数据模型而不编写迁移；否则必须提供迁移方案。
10. 不得自行假设可以删除、重命名或改变已有持久化字段类型。

---

## 样式与组件复用

1. 页面风格必须与项目现有页面保持一致。
2. 颜色、字体、间距、圆角、阴影等统一从 Design System 或 Theme 中获取。
3. 不得在业务页面中重复定义已有的颜色、字体或尺寸常量。
4. 项目不存在对应样式时，应在统一主题位置新增，不要散落在页面文件中。
5. 可跨页面复用的组件放入 `UI/Core/Components`。
6. 仅页面内部复用的组件放入该页面模块的 `Components` 或 `Views` 目录。
7. 通用组件必须保持业务无关，不得依赖具体页面的 ViewModel。
8. 新增或修改通用组件后，应同步更新：

   * 组件 Preview。
   * 示例页面。
   * 单元测试或快照测试。
9. 组件 API 应明确且稳定，避免暴露大量互相冲突的布尔参数。
10. 多种视觉状态优先使用枚举、配置结构或样式协议表达。
11. 图片和颜色资源优先放入 Asset Catalog。
12. 资源访问应使用项目已有的类型安全方案；没有类型安全生成工具时，至少集中管理资源名称。

---

## 本地化规则

1. 面向用户的固定文案必须进入本地化资源。
2. 使用项目现有的 String Catalog、`.strings` 或本地化生成方案。
3. 不得在 View、ViewModel 或业务逻辑中散落硬编码展示文案。
4. 带参数的文案应使用本地化格式化能力，不要通过字符串拼接生成句子。
5. 必须考虑复数、语序和不同语言下的布局长度。
6. 错误提示应由明确的错误类型映射为本地化文案。

---

## 测试规则

1. 新增或修改逻辑必须编写对应测试。
2. 修改完成后必须执行与改动相关的测试。
3. 测试失败时必须定位原因并修复，直到相关测试通过。
4. 未明确要求时，只运行与修改相关的测试目标或测试用例，避免无意义地执行全部测试。
5. 测试命令根据项目类型选择：

   * Swift Package 使用 `swift test`。
   * Xcode 项目使用 `xcodebuild test`。
   * 项目已有测试脚本时优先使用项目脚本。
6. ViewModel、Repository、Mapper、Use Case 和纯函数应优先编写单元测试。
7. 涉及页面交互、导航、表单提交或完整用户流程的修改，应编写 UI 测试。
8. 视觉变化明显的通用组件或页面，应按项目现有方案补充快照测试。
9. 测试不得依赖真实网络、生产数据库、系统当前时间或随机结果。
10. 时间、UUID、网络和持久化依赖应可注入并替换。
11. 异步测试不得使用任意固定时长的休眠等待结果。
12. 单个测试命令的超时时间统一限制为 1 分钟，除非项目已有明确的其他约定。
13. 不能在当前环境执行的测试必须明确列出，并说明未执行原因，不得声称已经通过。

---

## 构建与验证规则

1. 禁止通过启动应用代替静态检查和测试。
2. 未经明确要求，不要执行会启动模拟器或真机应用的命令。
3. 至少执行与修改相关的以下验证：

   * 编译检查。
   * 单元测试。
   * 格式或静态检查。
4. 根据项目情况使用：

   * `swift build`
   * `swift test`
   * `xcodebuild build`
   * `xcodebuild test`
   * SwiftLint
   * SwiftFormat
5. 使用 `xcodebuild` 前，应先检查可用的 workspace、project、scheme 和 destination，不得猜测名称。
6. 不得通过关闭警告、降低并发检查级别或删除测试来让构建通过。
7. 不得为了规避错误而扩大可选类型、添加强制转换或使用无意义的默认值。
8. 修改后应检查 `git diff`，确保没有无关改动、生成垃圾文件或意外格式化。

---

## 设计稿还原

根据图片或设计稿实现页面时，必须遵守以下原则。

1. 尽可能准确还原设计稿，不得自行改变明显的布局、层级和视觉关系。
2. 优先复用已有组件、字体、颜色和样式。
3. 可跨页面复用的组件应沉淀到 `UI/Core/Components`。
4. 可复用的样式应沉淀到 Design System 或 Theme。
5. 尺寸和间距优先遵循项目现有设计网格。
6. 项目明确采用 8 点网格时，新增的主要间距和尺寸应优先使用 8 的倍数。
7. 不要为了满足 8 点网格而错误修改设计稿中的图标尺寸、分割线宽度或系统控件尺寸。
8. 必须适配不同屏幕尺寸，不得只针对单一截图尺寸硬编码。
9. 必须检查：

   * 深色模式。
   * 动态字体。
   * 安全区域。
   * 横竖屏要求。
   * 无障碍。
   * 长文本和本地化文本。
10. 设计稿缺少交互状态时，应优先参考项目已有页面，不得自行创造与现有产品不一致的交互。
11. 设计稿中的资源不完整时，应明确列出缺失资源，不得用不相关素材冒充。

---

## 项目结构

以下目录结构为推荐结构。新增代码应放入对应分层位置，并保持 MVVM 与 DDD 边界清晰。

实际项目结构与本文不同时，应优先遵循项目现有结构，不得仅为匹配本文而进行大规模迁移。

```tree
App
├─ Application                         # 应用入口与全局配置
│  ├─ App                              # SwiftUI App 入口
│  ├─ DependencyInjection              # 全局依赖注册
│  └─ Configuration                    # 环境配置与功能开关
│
├─ Data                                # 数据层
│  ├─ Network                          # 网络客户端、请求与响应 DTO
│  ├─ Persistence                      # SwiftData、Core Data 或其他持久化实现
│  ├─ Repositories                     # Domain Repository 协议的具体实现
│  └─ Mappers                          # DTO、数据库实体与领域模型转换
│
├─ Domain                              # 领域层，不依赖 SwiftUI 和具体数据框架
│  ├─ Entities                         # 领域实体与值对象
│  ├─ Enums                            # 领域枚举
│  ├─ Repositories                     # Repository 协议
│  ├─ UseCases                         # 可复用业务用例
│  └─ Errors                           # 领域错误
│
├─ Localization                        # String Catalog 与本地化资源
│
├─ Routing                             # 路由、深链与导航协调
│
├─ UI                                  # 表现层
│  ├─ Features                         # 按功能或页面划分
│  │  └─ ExampleFeature
│  │     ├─ State                      # 页面状态与展示模型
│  │     ├─ Views                      # 页面主 View
│  │     ├─ ViewModels                 # 页面 ViewModel
│  │     └─ Components                 # 页面内部组件
│  │
│  ├─ Core
│  │  ├─ DesignSystem                  # 颜色、字体、间距、样式
│  │  ├─ Components                    # 跨页面通用组件
│  │  └─ Previews                      # 通用组件示例与 Preview 数据
│  │
│  └─ Shared                           # 共享容器、Tab、通用页面结构
│
├─ Resources                           # Assets、配置文件及其他资源
│
└─ Utilities                           # 无业务含义的通用工具
```

---

## 页面模块结构

每个页面或功能模块原则上包含以下目录：

```tree
ExampleFeature
├─ State
├─ Views
├─ ViewModels
└─ Components
```

各目录职责如下：

* `State`

  * 页面状态。
  * 展示模型。
  * 页面事件或动作类型。
* `Views`

  * 页面入口 View。
  * 仅负责渲染和事件转发。
* `ViewModels`

  * 页面状态管理。
  * 异步操作。
  * Repository 或 Use Case 调用。
* `Components`

  * 仅供当前功能模块使用的子 View。
  * 不应被其他模块直接依赖。

简单页面不要求机械创建所有目录。只有单个文件且职责清晰时，可以保持扁平结构。

---

## 重要约束与落位规则

### Repository Only Rule

* `UI` 和 `ViewModel` 禁止直接使用数据库、持久化上下文、DAO 或网络客户端。
* UI 层只能调用 Domain 层定义的 Repository 协议或 Use Case。
* 数据库和网络访问仅限 Data 层。
* Repository 实现负责将外部数据转换为领域实体。
* Domain 层不得依赖 Data 层。

### MVVM

* View 只负责渲染和转发交互。
* 页面状态、业务判断和异步逻辑放在 ViewModel。
* 可跨页面复用的业务逻辑放入 Use Case 或领域服务。
* ViewModel 不得承担数据库实体映射等 Data 层职责。

### 样式与组件复用

* 颜色、文字、间距、圆角等统一从 `UI/Core/DesignSystem` 获取。
* 可复用组件放入 `UI/Core/Components`。
* 页面专属组件放入对应 Feature 的 `Components`。
* 通用组件应能够脱离具体业务页面独立使用。
* 新增或修改通用组件时，必须同步维护 Preview、示例和测试。

### 文案与多语言

* 面向用户的固定文案优先放入 `Localization`。
* 按项目现有流程生成或更新本地化访问代码。
* 不得通过字符串拼接构建需要翻译的完整句子。

---

## 新增功能放置指引

* 领域模型：`Domain/Entities`
* 领域枚举：`Domain/Enums`
* 领域错误：`Domain/Errors`
* Repository 协议：`Domain/Repositories`
* Use Case：`Domain/UseCases`
* 网络 DTO：`Data/Network`
* 数据库模型：`Data/Persistence`
* Repository 实现：`Data/Repositories`
* 数据映射：`Data/Mappers`
* 页面与 ViewModel：`UI/Features/<FeatureName>`
* 页面内部组件：`UI/Features/<FeatureName>/Components`
* 通用组件：`UI/Core/Components`
* 主题和设计规范：`UI/Core/DesignSystem`
* 路由与深链：`Routing`
* 本地化资源：`Localization`

---

## 修改范围规则

1. 只修改完成当前任务所需的文件。
2. 不得修改无关业务逻辑。
3. 不得无理由调整公共 API。
4. 不得无理由升级依赖、最低系统版本、Swift 版本或 Xcode 工程配置。
5. 不得删除现有兼容逻辑，除非需求明确要求。
6. 不得通过大规模重构掩盖局部问题。
7. 确需重构时，应保证：

   * 重构与功能修改边界清晰。
   * 行为保持不变。
   * 有测试覆盖。
   * 改动范围可审查。
8. 发现潜在问题但不属于当前任务时，只记录问题，不要顺手修改。

---

## 完成标准

任务只有在满足以下条件后才算完成：

1. 需求中的功能已经实现。
2. 代码放置在正确的架构层级。
3. 没有直接跨越 Repository、Domain 或 UI 边界。
4. 没有修改无关文件。
5. 相关代码能够编译。
6. 相关测试已经执行并通过。
7. 新增或修改的逻辑有对应测试。
8. 通用组件的 Preview、示例和测试已同步更新。
9. 本地化、深色模式、动态字体和无障碍要求已按任务范围检查。
10. 已检查最终 diff，不包含临时文件、调试代码或意外修改。
11. 未执行的验证项目及其原因已明确说明。
